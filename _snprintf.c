#include "shell.h"

/**
 * _snprintf - It is typically used to write formatted data to a string buffer instead of printing it to the standard output.
 * @str: buffer.
 * @size: size_t
 * @str1: arguments to be added to append to the buffer
 * @str2: arguments to be added to append to the buffer
 *
 * Retunr: 0 on succeess, -1 on fail.
 */
int snprintf(char *str, size_t size, char *str1, char *str2)
{
	
} 
